
        count: integer = 0;
        loop_cond: boolean = true; 
        main: function void(){
            while(loop_cond)
                if (count % 12 == 0) continue;
            do{
                // nullable
            } while (count <= true || false);
            return arr[main(foo(arr[main()]))];
        }
        